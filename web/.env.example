# Local dev — points at the FastAPI server.
VITE_API_BASE_URL=http://localhost:8000

# --- Production build example ---
# Build-time env vars are inlined by Vite, so set this in your CI / deploy env:
# VITE_API_BASE_URL=https://api.example.com
